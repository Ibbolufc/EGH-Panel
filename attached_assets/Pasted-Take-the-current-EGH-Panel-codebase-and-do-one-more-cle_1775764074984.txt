Take the current EGH Panel codebase and do one more cleanup pass focused only on **self-hosting polish and deployment quality**. Do not redesign the app or change branding. Keep the current stack if you want. The goal is to make this version cleaner, less Replit-tied, and easier to deploy on a normal Ubuntu server.

## Main goals

### 1. Remove remaining Replit-specific dependencies

Please remove the remaining Replit-specific frontend dependencies and any leftover Replit-only assumptions if they are not truly required for self-hosting.

I want the project to be as platform-neutral as possible.

That includes:

* removing unnecessary Replit plugins/packages
* removing leftover Replit project baggage where safe
* making sure the app does not rely on Replit-specific environment variables or runtime behavior

If anything must remain, document exactly why.

### 2. Make deployment more complete

Improve the deployment flow so a normal self-hosted install is smoother.

I want:

* a cleaner first-time deployment flow
* clearer separation between first install and normal updates
* no confusing manual gaps

Please improve this so:

* database setup is handled clearly
* seed flow is handled clearly
* first install steps are obvious
* update/redeploy steps are obvious

### 3. Make seeding cleaner

Right now the seed process still feels a bit awkward.

Please improve it so that:

* seeding can be run cleanly in a self-hosted environment
* it is documented properly
* it works well with Docker-based deployment
* first-time demo setup is simple and reliable

If possible, make the seed flow Docker-friendly rather than relying on host machine tooling.

### 4. Improve deploy scripts

Please review and improve the deployment scripts so they are reliable for:

* first install
* later updates
* rebuilding containers
* migrations
* optional seeding

If appropriate, create clearly named scripts such as:

* first-time setup
* standard deploy/update
* seed demo data

Keep them simple and well documented.

### 5. README cleanup

Update the README again so it is very clear and honest.

I want it to include:

* exact deployment steps for Ubuntu
* exact first-time install steps
* exact update/redeploy steps
* how to seed demo data
* how to skip seed data in production
* what parts of the system are still mocked
* what parts are real
* what environment variables are required
* whether HTTPS is included or expected behind an external reverse proxy

Make sure every command in the README matches the real repo exactly.

### 6. Final self-hosting verification

Please do a final cleanup and verification pass for:

* Docker build flow
* Docker compose flow
* migrations
* seed flow
* demo login flow
* admin pages
* client pages
* environment variable docs

## Important

* do not rewrite the project unnecessarily
* do not remove working features
* do not give a marketing answer
* make the codebase cleaner and the deployment story better
* keep the result GitHub-friendly and self-hostable

## At the end

When finished, tell me clearly:

1. what Replit-specific pieces were removed
2. what deployment/seeding improvements were made
3. what commands I should run on a fresh Ubuntu server
4. what commands I should run for later updates
5. what still remains mocked or scaffolded
